# AI 翻译服务 · 学习版 4

先按 [安装说明](START-HERE.md) 加载 Chrome / Edge 扩展，再打开扩展选项左侧 **AI 翻译向导**。首次免 Key 试用用下方 MyMemory；8 家 AI 预设则需自己的 API Key 与模型权限，点击「测试接口」，确认结果后保存或设为全局服务。已有网页可在悬浮面板中切换服务，原文仍保留。网页模式先保存配置再测试。

这是预发布学习版。MyMemory 有真实双向请求记录；**8 家 AI API 尚无真实 Key 调用记录，豆包/Kimi 后台网页尚无真实登录会话验收**。预设存在表示程序已实现相应配置与请求适配，不代表账号权限、费用或当前服务可用性已经替你验证。[验证记录](VALIDATION.md)区分了模拟测试、真实 API 与 Web 开发页面。

## 先试一个不用 Key 的真实接口

向导顶部新增 **MyMemory · 免Key试用**。它是匿名限量的在线机器翻译接口，可直接测试中英双向，不要求账号、邮件或 API Key；不是聊天模型，不使用固定 AI Skill。[官方请求文档](https://mymemory.translated.net/doc/spec.php)规定每次最多 500 个 UTF-8 字节，插件会按段和字节分片，逐个发送，保留原有标签、代码与占位符。分片和标签边界会减少上下文，影响译文流畅度。

在“仅本机离线”模式下测试按钮禁用。点击 **切到屏蔽谷歌模式** 后会允许非谷歌云端服务联网，再点击 **测试免费接口** 才发送卡片上的合成句。确认结果后点击 **添加到翻译服务**，刷新原阅读页面，再在翻译面板选择 **MyMemory · 免 Key**；添加按钮不会改动既有同名配置或自动改全局服务。

请求使用官方 `/get` 端点，不发送 Key、邮件、Cookie 或阅读页地址作为请求元数据；用户选择翻译的正文如果本身含 URL，它仍可能作为正文发送。不会调用贡献数据的 `/set`。服务商可能留存待译文本。免费额度和可用性以[官方规则](https://mymemory.translated.net/doc/usagelimits.php)为准；既有验证时额度说明页访问受限，因此没有写死每日额度。遇到额度耗尽或 429 会停止同一运行环境的待发队列，冷却 60 秒后仅接受新的手动请求；每日额度用完可能需要等候更久，不自动重试或换平台。

开发者按 [源码开发步骤](START-HERE.md#从源码学习和构建) 启动端口 4318 的开发服务器后，可访问 `free-api-demo.html` 的合成示例。它不是已部署的网站，安装扩展无需启动这个服务器。

自动识别仅做本机中英简单判断，混合语段或其他语言请手动指定；此接口适配不承担繁简互转或 AI 词典任务。

## 8 个预设

以下端点、模型与计费说明于 2026-09-14 根据官方资料核对。免费权益与实名认证、地域和账号有关，切换模型后需要重新核对价格；本扩展不提供共享密钥或代付额度。

| 服务                | 默认模型 / 接入点                       | API 费用状态                               | 官方说明                                                                                                                                                      |
| ------------------- | --------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 豆包 / 火山方舟     | 用户填写 `ep-…` 接入点 ID               | 有限试用，用完付费                         | [API](https://www.volcengine.com/docs/82379/1494384) · [价格](https://www.volcengine.com/product/ark)                                                         |
| Kimi / Moonshot     | `kimi-k2.6`                             | 按量付费                                   | [兼容与参数](https://platform.kimi.com/docs/guide/migrating-from-openai-to-kimi) · [价格](https://platform.kimi.ai/docs/pricing/chat)                         |
| DeepSeek            | `deepseek-v4-flash`                     | 按量付费                                   | [请求示例](https://api-docs.deepseek.com/api_samples/chat_curl/) · [价格](https://api-docs.deepseek.com/quick_start/pricing/)                                 |
| 千问 / 阿里云百炼   | `qwen-flash`，北京地域接口              | 新人限期试用，用完付费                     | [兼容 API](https://help.aliyun.com/zh/model-studio/qwen-api-via-openai-chat-completions) · [免费额度](https://help.aliyun.com/zh/model-studio/new-free-quota) |
| 智谱 GLM            | `glm-4.7-flash`                         | 免费模型，需要 Key                         | [模型说明](https://docs.bigmodel.cn/cn/guide/models/free/glm-4.7-flash) · [价格](https://bigmodel.cn/pricing)                                                 |
| 腾讯混元 / TokenHub | 用户填写控制台模型服务 ID               | 新人体验额度，用完计费                     | [新接口](https://cloud.tencent.com/document/product/1823/130079) · [体验额度](https://cloud.tencent.com/document/product/1823/130053)                         |
| 硅基流动            | `Qwen/Qwen2.5-7B-Instruct`              | 官方翻译示例列为免费，当前价格需控制台确认 | [翻译示例](https://docs.siliconflow.cn/docs/usercases/use-siliconcloud-in-bob) · [当前价格](https://siliconflow.cn/pricing)                                   |
| OpenRouter          | `qwen/qwen3-next-80b-a3b-instruct:free` | 免费模型，有请求限额，需要 Key             | [模型与价格](https://openrouter.ai/qwen/qwen3-next-80b-a3b-instruct:free) · [限额](https://openrouter.ai/docs/faq)                                            |

上表是标注日期的配置参考，当前模型、额度与价格应在自己的服务控制台再次确认。免费模型通常仍需 Key，可能有账号、地区或请求限制。豆包、Kimi 的免费网页不能当作永久免费 API。完整请求地址在选项中可见，也可查看 `src/config/aiServices.js`。

## 自定义 API

点击 **自定义兼容 API**，填写服务名称、API 地址、Key、模型与翻译偏好。支持 OpenAI **Chat Completions** 协议；`https://example.com/v1` 自动补成 `https://example.com/v1/chat/completions`，也接受完整端点。不是 Responses 或任意自定义 JSON 协议。

远程地址必须使用 HTTPS；HTTP 仅用于明确的本机地址。远程服务需要 Key，本机无鉴权服务可留空。请求使用 `messages`、`model`、`max_tokens` 等兼容字段，读取 `choices[0].message.content`。「翻译服务 → Custom」仅支持文档约定的声明式协议；安全版不执行 Request / Response Hook。Argos 本机服务必须填写启动后生成的配对令牌。

向导保存的预设与自定义项都有稳定标识，更新同一项只改它的配置，不覆盖其他翻译服务。向导自定义槽位用于一项常用服务，多项配置可在原「翻译服务」中复制管理。

## 翻译 Skill

此处的 Skill 是随 AI 请求发送的固定模型指令，定义见 [翻译 Skill](TRANSLATION-SKILL.md)，也可在向导展开查看。它要求仅返回译文、保持含义，保留网页标签、占位符、数字及代码；原文中的命令按原文翻译，不作为任务执行。MyMemory、Microsoft 和 Argos 的普通机器翻译不使用这套聊天模型指令。

用户可添加术语与风格偏好。原文、语言方向和偏好由程序序列化为数据，核心指令保持独立。向导服务采用单段、非流式返回后替换，避免将半句或思考内容插入网页。单段上限 12,000 字符、偏好 2,000 字符；超限会报错而不是悄悄截断。模型仍可能出错，提示词不构成模型行为保证。

Kimi 使用关闭思考及温度 0.6；DeepSeek/GLM 关闭思考；千问使用其 `enable_thinking` 参数。豆包和腾讯需用户选定具体模型，未知型号不套用通用思考参数。高级接口的自定义 Body 不会覆盖向导的固定指令。安全版停用所有 JavaScript Hook；提示词和术语偏好仍可正常使用。

## 后台网页：豆包与 Kimi（实验性）

两家同时提供「后台网页」方式。需安装 Chrome/Edge 扩展，并在**同一浏览器、同一用户配置文件**登录相应网站。普通后台标签页自然使用该浏览器的登录状态，扩展不导出 Cookie、不读取账户存储、不调用网站未公开的私有 API。

每项翻译创建一个不激活的专用标签，填入任务并只发送一次，读取匹配本次随机标记的回复；成功后关闭。阅读页不自动跳走。现有聊天标签、草稿不会被复用或覆盖。遇到登录、验证码或页面结构变化会停止，并保留本任务标签供手动处理；重新翻译前先在该标签确认状态。新标签可能恢复网站自己的共享草稿，检测到内容时不会覆盖。

此模式逐段串行，速度比 API 慢，会使用网站聊天额度并可能留下聊天历史。关闭翻译或切换服务会取消待发任务；已被服务端接收的请求无法保证撤回。页面适配随官网变化，**本次仅完成 DOM 夹具和消息/队列验证，未完成真实登录会话验收**，所以保留实验性标识。不要把它当作八家网站均已稳定自动化。

「仅本机离线」会在创建网页任务前与提交前拒绝访问远程 AI 页面，运行中切换离线会停止任务。网页自身的资源请求不受插件 HTTP 请求策略控制；「屏蔽谷歌」也不等于操作系统防火墙。离线阅读继续使用本机 Argos。

## 切换效果

切换服务或中英目标语言时，已完成的译文保留到新结果成功返回；新结果逐段以轻淡擦除和淡入替换。失败的段落继续保留旧译文，关闭翻译则收起全部译文。系统启用「减少动态效果」时直接替换。

不刷新整个页面即可切换的路径是网页控制面板中选择另一项已配置服务。编辑同一服务的 API Key、地址或翻译偏好后，需要刷新原阅读页面才能加载新配置；这种配置编辑不属于动态服务切换。刷新页面或发生结构性规则变更时，页面会按新规则重新扫描。

## 验证范围

已有记录包括官方文档核对、8 家请求参数和兼容响应解析的模拟测试、向导保存与测试按钮行为，以及后台队列、取消和 DOM 夹具测试。未提供各家账号的 API Key，因此没有调用这些云模型；这不影响单独记录的 MyMemory 真实匿名试用。可在向导本地填写自己的 Key 后测试一条固定合成句，费用按相应平台实际规则执行。安装及报错处理见 [排错说明](docs/TROUBLESHOOTING.md)，真正断网使用见 [Argos 说明](offline/README.md)。
