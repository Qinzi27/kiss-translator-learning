# 开始使用：一键双语翻译学习版

在普通网页上单击悬浮翻译按钮，原文保留，译文追加在下一行；再次单击收起。项目基于 [KISS Translator](https://github.com/fishjar/kiss-translator)，支持在线服务与本机 Argos 中英模型切换。

## 1. 下载并安装到 Chrome / Edge

本说明对应 **2.0.37-learning.8**。从 [GitHub Releases](../../releases) 选择同名预发布，下载 `kiss-translator-learning-chrome.zip`。Chrome 和 Edge 使用同一安装包；如果只下载了 GitHub 的 Source code ZIP，须先按本文末尾构建，不能直接加载源码根目录。此版标为预发布，后台网页方式仍属实验性；Chrome 内文件夹更新和本次跨页锁定的完整流程仍需真实安装环境验收。

可选：下载同一发布页的四个附件（Chrome ZIP、对应源码 ZIP、`release-manifest.json`、`SHA256SUMS.txt`），放在同一目录核对完整性：macOS 用 `shasum -a 256 -c SHA256SUMS.txt`，Linux 用 `sha256sum -c SHA256SUMS.txt`。Windows PowerShell 用 `Get-FileHash .\kiss-translator-learning-chrome.zip -Algorithm SHA256`，将结果与校验文件中的同名条目比较；源码 ZIP 和 `release-manifest.json` 也同样核对。只下载单个 ZIP 时，可只计算该包的 SHA-256 并比对对应条目。

1. 将 ZIP 解压到一个准备长期保留的文件夹。里面的 **`chrome` 文件夹**应直接包含 `manifest.json`、`content.js`、`background.js` 和 `options.html`。
2. Chrome 打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
3. 打开「开发者模式」，点击「加载已解压的扩展程序」，选择上面的 **`chrome` 文件夹**。不要选择 ZIP 或它外面的文档目录。
4. 将扩展固定到工具栏。通过扩展详情中的「扩展程序选项」打开设置，也可从工具栏弹出面板进入设置。
5. 刷新一篇已经打开的普通英文网页，点击网页边缘的翻译悬浮按钮；再次点击应收起译文。快捷键默认 `Alt + Q`，macOS 为 `Option + Q`；若被占用，可在浏览器扩展快捷键页面调整。

这是供手动加载的学习版，不是 Chrome / Edge 商店安装包。加载后应保留原解压目录；移动或删除目录会影响扩展重新加载。只使用在线翻译不需要安装 Python、Node.js 或启动开发服务器。

从旧版升级后，先在扩展管理页重新加载，再刷新已经打开的阅读标签。learning.8 的锁定默认关闭，原按钮位置、三种颜色和点击偏好保留。

本地 HTML 需在扩展详情中开启「允许访问文件网址」。浏览器设置页、扩展商店等受保护页面不能作为整页翻译验收页。同一页面同时运行多个整页翻译扩展可能产生重复译文。

## 直接阅读和翻译 PDF

**Chrome 151+ 且官方 MIME 接口可用时：** 打开本地 PDF 或在线 PDF，地址栏保留原地址，页面直接显示本版阅读器。只接管顶层 PDF，不接管网页内嵌文档。默认仅在本机读取原文，点击 **翻译本页** 或其他翻译按钮后才调用当前服务。这个原生流入口不需要开启文件网址权限，也不需要 Python 阅读服务。

页面可取消勾选 **打开 PDF 时直接使用双语阅读器**，使之后打开的 PDF 恢复浏览器默认方式；点击 **返回 Chrome 原生阅读器** 可切回当前文件。接管能力见 [Chrome 官方文档](https://developer.chrome.com/docs/extensions/reference/api/mimeHandler)。Chrome 153 已安装在开发机器上，但本版的这条 MIME 路径尚未完成真实安装环境验收；不要把下面的使用步骤当作已实测结果。

**旧浏览器或关闭接管时：** 在 PDF 标签点击扩展菜单 **翻译当前 PDF**，或按 `Alt / Option + Q`，同标签读取并翻译当前页。直接读取本地 `file:///` 地址需允许扩展访问文件网址；也可从设置中的 **PDF 双语阅读** 打开阅读器，点击 **选择 PDF 并翻译** 重选该文件，无需更改权限。只想读原文时选 **打开本地 PDF**。原地址含 `#page=N` 时使用该页，否则从第一页开始；扩展不能读取原生阅读器内部滚动位置。

点击 **翻译本页** 也会开启 **翻页自动翻译**，优先处理当前页并预翻译后两页，最多并发 2 个请求，会使用所选服务的额度。翻页重排待处理任务，保留已在途请求。停止、服务报错或换文件会终止该轮处理，保留已完成结果并忽略迟到回复，不会自动换平台。全文翻译会退出翻页模式，优先当前页，再处理其余页；缓存命中时跳过请求。

译文采用会话内 LRU 缓存，合计最多 80 页、8 MiB。刷新后重新读取相同文件，可按相同服务配置恢复缓存，但不会自动续发请求；手选文件需重选同一文件。扩展使用 `storage.session`，浏览器会话结束清除。开发预览的 `sessionStorage` 在恢复标签时可能保留，不能视为同等生命周期保证。切换服务后旧译文仍保留，需要主动重新翻译。完整说明与验证边界见 [PDF 阅读说明](docs/PDF-READER.md)。

## 2. 先用 MyMemory 免 Key 试一句

MyMemory 是**有额度的在线机器翻译接口**，不需要账号、邮件或 API Key；它不是聊天 AI，也不是离线服务，不使用 AI 翻译 Skill。

1. 打开扩展设置，进入左侧 **AI 翻译向导**，找到顶部 **MyMemory · 免Key试用**。
2. 选择「英文 → 中文」或「中文 → 英文」，点击 **测试免费接口**。只会发送卡片中展示的合成句。
3. 若当前为「仅本机离线」，先点击 **切到屏蔽谷歌模式**。这一步允许非谷歌远程服务联网；仍需再次点击测试才发送示例句。
4. 测试成功后点击 **添加到翻译服务**。若提示已经在列表中，无需重复添加。
5. 刷新原阅读页面，打开工具栏翻译面板，选择 **MyMemory · 免 Key**，再点击网页悬浮按钮。测试与添加操作都不会自动更改全局默认服务。

首次安装的默认在线服务仍是上游 Microsoft，也可直接试用。选择哪个在线服务，待译文本就会发送到哪个服务。免费服务可能限流或变化，不能保证无限使用。MyMemory 额度耗尽或返回 429 时会停止本运行环境的待发队列；至少冷却 60 秒后可手动重试，每日额度用完可能需要等待更久。不会自动换平台。

8 家 AI API、自定义接口、费用说明及豆包/Kimi 实验性网页方式见 [AI 翻译服务说明](AI-SERVICES.md)。其中 8 家 API 尚未用用户真实 Key 调用，后台网页方式尚未完成真实登录会话验收。

## 3. 设置默认服务与双语显示

只调整当前页面：在网页翻译面板中选择已配置的服务或目标语言。已完成的旧译文会保留，等新译文成功后逐段擦除淡入；失败时保留旧文，并在段落上显示简短错误提示。系统开启「减少动态效果」时直接替换。

调整以后打开网页使用的默认值：

1. 扩展设置 → **网页翻译 → 全局规则**。
2. 点击规则底部的 **编辑**，选择服务、原文语言与目标语言。
3. 保持「仅显示译文」关闭，点击 **保存**。
4. 回到阅读页面并刷新。某个网站已有专用规则时，它可能覆盖全局规则，可在当前网页面板再次选择。

编辑同一服务的 Key、地址、模型或翻译偏好后，需要刷新原阅读页面才会载入新配置；这种配置编辑不属于动态服务切换。新安装默认使用简体中文界面、原文自动判断、目标简体中文，且不自动开启整页翻译。已有的按钮操作、界面和服务偏好会保留，升级不会强制覆盖。

### 看懂并调整侧边翻译按钮

网页与 PDF 按钮按真实处理状态显示进度环，完成时显示勾号，出错时显示错误图标；鼠标停留可查看状态。PDF 翻译中的方形图标可以停止处理，网页按钮保留已设置的直接翻译 / 菜单操作。停止后保留已完成译文；「翻页自动翻译」开关本身不会让 PDF 按钮一直显示忙碌。

网页按钮从首次绘制就使用已保存的位置，避免先在角落出现再移动。打开新网页时，扩展仍需向该新文档重新注入按钮，位置和颜色会沿用；切换目标语言只调整当前页翻译。

进入 **概览 → 悬浮翻译按钮颜色**，分别选择空闲、翻译中、完成颜色，或输入 `#RRGGBB` 六位色值。有效颜色会自动保存并同步到已打开的网页和 PDF；未更新时刷新阅读页。输入不完整或格式错误时不会保存；图标自动选高对比黑 / 白色。点击 **恢复默认颜色** 只改三种颜色，不动位置、隐藏设置和点击方式。系统开启减少动态效果时，状态仍可辨认，进度环不旋转。

### 长按：决定以后打开的网页是否自动翻译

1. 在普通网页上按住悬浮按钮约 **650 毫秒**，直到出现小锁徽标。锁定成功后会启用当前页翻译，后续普通顶层网页、刷新和单页应用的新路径也会自动翻译。
2. 默认普通单击仍只开关当前页，不改变锁定。保留旧「单击打开菜单」偏好的用户仍可从菜单操作；暂停当前页后，小锁可以继续保留。
3. 再次长按关闭锁定。当前页已有译文保留，后续新网页等待手动操作。即使旧网站 / 全局规则中曾保存自动开启翻译，未锁定时也不会自动开启新网页。
4. 键盘操作：将焦点移到按钮，按 **向下箭头**或 **Shift + F10**，在菜单中选择 **锁定持续翻译（新网页自动翻译）**或 **关闭持续翻译锁定**。

拖动、提前松手、指针取消或失去焦点会取消未完成的长按；成功长按后的松手不会再单击一次。按钮标题与状态播报会说明锁定状态；若提示「锁定设置未保存，请重试」，请按仍显示的锁定状态判断，重新操作即可。

锁定保存在当前浏览器配置的本机持久设置中，使用独立存储项，不会被按钮拖动或颜色保存覆盖。其他已经打开的标签只同步锁标记，不会立即发送其中的文本；之后刷新或进入新路径时再按锁定决定。网站黑名单与联网策略仍生效；iframe 和 PDF 不受网页锁定的自动开启控制，PDF 继续使用自己的翻译及翻页选项。选用在线服务时，锁定会让之后符合条件的网页自动使用该服务和额度。

本轮自动回归覆盖位置、长按、拖拽取消、键盘操作及合成事件拒绝；它们不能代替真实 Chrome 中完整的跨页使用验收，也没有新增云模型 Key 或 API 的实调验证。详见 [验证记录](VALIDATION.md)。

## 4. 选择联网方式

在扩展设置 **概览 → 联网策略** 中选择：

| 方式       | 用途                                                                               |
| ---------- | ---------------------------------------------------------------------------------- |
| 正常联网   | 使用所选在线或本机服务                                                             |
| 屏蔽谷歌   | 拒绝谷歌请求，仍允许其他远程服务；可选择 Microsoft、MyMemory 等                    |
| 仅本机离线 | 插件内置 HTTP 请求层只允许明确的本机回环地址；使用已准备好的 Argos，远程服务会报错 |

“屏蔽谷歌”下 Microsoft 双向成功已有真实测试，但不代表中国所有运营商都可访问。“仅本机离线”约束插件请求层，不是整台电脑的防火墙，也不改变网页自身联网或撤回此前已发出的请求。切换策略后刷新阅读页；真正断网阅读还需提前打开或保存文章。

## 5. 真正离线：准备本机 Argos

**扩展安装包、源码包都不包含 Python 依赖与翻译模型。** 安装包根目录附带 `offline` 脚本；也可下载同一发布版本的 `kiss-translator-learning-source.zip`。在脚本所在项目先联网完成准备，再断网使用；只加载浏览器扩展不会执行这些准备步骤。初次准备、日常启动、停止、换机及 Windows 命令见 [离线使用说明](offline/README.md)。

准备并验证成功后，每次需要本机服务时，在源码项目根目录运行：

```sh
.offline/venv/bin/python offline/server.py
```

Windows PowerShell 使用：

```powershell
.\.offline\venv\Scripts\python.exe offline\server.py
```

首次启动会生成配对令牌；复制终端显示的令牌到「翻译服务 → 本机离线 · Argos」的 **Key** 并保存（旧版空 Key 也须补填）。令牌只保存在本机，不要公开。

保持终端运行，扩展中选择 **仅本机离线** 与 **本机离线 · Argos**。固定接口地址为 `http://127.0.0.1:8765/translate`，Key 填写本机配对令牌。这是本机 API 地址，不是扩展安装网址。关闭终端或按 `Ctrl + C` 会停止服务；下次重新运行同一命令即可，不必重新下载模型。

只有中英模型，混合段落可手动选源语言。标签分段会影响上下文、语序和流畅度。Ollama 接口沿用上游，但本学习版未安装或实测，不能把云模型当作离线模型。

## 更新、备份与排错

升级前保留自己的设置备份，设置导出不包含 Key。Chrome 开发者模式安装可在设置左侧进入 **插件更新**：

1. 首次点击 **选择并授权安装目录**，选择 Chrome 正在加载、直接包含 `manifest.json` 的 `chrome` 文件夹；从源码安装则选择 `build/chrome`。不能选择同版本副本、外层目录或源码根目录。
2. 浏览器要求时允许目录读写。插件会写入随机纯文本探测文件，从本扩展地址读回匹配后删除，用于确认确实选中了当前安装目录；这一步不会下载更新。
3. 点击 **检查更新**，发现新版后点击 **下载并更新**。保持设置页打开，等待下载、校验、备份和逐文件写入完成。
4. 点击 **重新加载插件**，再刷新网页 / PDF 阅读标签。写入文件本身不会自动重启扩展。

浏览器内更新不需要 Python、Git、Node.js 或 Token，也不新增扩展清单权限；仍需首次文件夹授权。旧文件内容最多 64 MiB，以一份备份保存在本扩展的 IndexedDB。**逐文件更新不是整目录原子替换**，错误时会尝试恢复；关闭页面、失电或其他程序修改安装文件后，可能需要重新进入更新页，点击 **恢复未完成更新**，恢复成功后再重新加载。恢复不会回滚浏览器设置。这个 Chrome 文件授权 / 磁盘更新完整流程尚未实机验收，当前自动测试使用合成文件系统与存储夹具。

检查和下载仅访问固定仓库的公开 GitHub 发布及允许的附件 CDN 跳转，不携带登录 Cookie 或翻译 Key。浏览器内「仅本机离线」会禁止联网更新，恢复备份不需要联网。

原 Python 工具继续保留：macOS 双击 `chrome` 文件夹旁的 **更新插件.command**，Windows 双击 **更新插件.bat**，需要 Python 3.9+。**不要与浏览器更新同时运行**，它不受扩展离线策略控制，备份也独立。两种方式的权限、限额、恢复步骤见 [UPDATING.md](UPDATING.md)。只重新加载通常会保留设置；卸载、换浏览器用户配置文件或换电脑则不能依赖原本地存储。不要为了补一个缺失预置就清空全部设置。

按钮不出现、服务列表未更新、免费额度错误、本机连接失败等，见 [排错说明](docs/TROUBLESHOOTING.md)。已验证内容与限制见 [验证记录](VALIDATION.md)。

## 从源码学习和构建

以下仅面向开发者。包含 PDF.js 的当前源码以 Node.js 24、pnpm 11 和项目锁文件为构建参考；依赖安装需要联网。在源码根目录运行：

```sh
pnpm install --frozen-lockfile
pnpm build:chrome
```

然后按安装步骤加载生成的 `build/chrome`。本地 Web 开发演示须先运行下面的命令，才能访问所列地址；它们不是 GitHub 发布后的在线网站，也不是扩展使用所需服务：

```sh
HOST=127.0.0.1 PORT=4318 BROWSER=none pnpm start
```

开发页面：`http://127.0.0.1:4318/content.html`、`http://127.0.0.1:4318/free-api-demo.html`。Web 开发存储与已安装扩展的存储独立，后台消息、权限和跨域行为也不同。Windows PowerShell 可分别设置 `$env:HOST='127.0.0.1'`、`$env:PORT='4318'`、`$env:BROWSER='none'`，再运行 `pnpm start`。

阅读入口：`src/views/Action/ContentFab.js` → `src/libs/translatorManager.js` → `src/libs/translator.js` → `src/apis/trans.js`。固定模型指令见 [翻译 Skill](TRANSLATION-SKILL.md)。

基础为 KISS Translator 2.0.32，提交 `2656f564dde5b271a8d3fb31e951a8457e8df41c`。学习版继续采用 GPL-3.0，分发扩展时同时提供对应源码和 `LICENSE`；源码 ZIP 不含 Git 历史。沉浸式翻译仅用于交互参考，没有复制其当前产品实现。

## 安全版升级差异

旧版用户先看 [SECURITY.md](SECURITY.md)。自定义 JavaScript Hook 仅保存、不执行；翻译接口的 HTTP 仅接受明确回环地址，远程端点须 HTTPS 且不能重定向。浏览器更新的公开 GitHub 附件跳转是单独受限的下载路径，见 [UPDATING.md](UPDATING.md)。设置导出/同步不再包含密钥、提示词或自定义请求配置；新设备需要补填。旧明文云备份会被拒绝；须按安全说明取出其中的设置对象、核实并显式导入本机，再使用新的空同步位置建立加密同步。
