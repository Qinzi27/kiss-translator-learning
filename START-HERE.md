# 开始使用：一键双语翻译学习版

在普通网页上单击悬浮翻译按钮，原文保留，译文追加在下一行；再次单击收起。项目基于 [KISS Translator](https://github.com/fishjar/kiss-translator)，支持在线服务与本机 Argos 中英模型切换。

## 1. 下载并安装到 Chrome / Edge

从 [GitHub 预发布 v2.0.32-learning.3](https://github.com/Qinzi27/kiss-translator-learning/releases/tag/v2.0.32-learning.3) 下载 `kiss-translator-learning-chrome.zip`。Chrome 和 Edge 使用同一安装包；如果只下载了 GitHub 的 Source code ZIP，须先按本文末尾构建，不能直接加载源码根目录。此版标为预发布，后台网页方式仍属实验性。

可选：下载同一发布页的四个附件（Chrome ZIP、对应源码 ZIP、`release-manifest.json`、`SHA256SUMS.txt`），放在同一目录核对完整性：macOS 用 `shasum -a 256 -c SHA256SUMS.txt`，Linux 用 `sha256sum -c SHA256SUMS.txt`。Windows PowerShell 用 `Get-FileHash .\kiss-translator-learning-chrome.zip -Algorithm SHA256`，将结果与校验文件中的同名条目比较；源码 ZIP 和 `release-manifest.json` 也同样核对。只下载单个 ZIP 时，可只计算该包的 SHA-256 并比对对应条目。

1. 将 ZIP 解压到一个准备长期保留的文件夹。里面的 **`chrome` 文件夹**应直接包含 `manifest.json`、`content.js`、`background.js` 和 `options.html`。
2. Chrome 打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
3. 打开「开发者模式」，点击「加载已解压的扩展程序」，选择上面的 **`chrome` 文件夹**。不要选择 ZIP 或它外面的文档目录。
4. 将扩展固定到工具栏。通过扩展详情中的「扩展程序选项」打开设置，也可从工具栏弹出面板进入设置。
5. 刷新一篇已经打开的普通英文网页，点击网页边缘的翻译悬浮按钮；再次点击应收起译文。快捷键默认 `Alt + Q`，macOS 为 `Option + Q`；若被占用，可在浏览器扩展快捷键页面调整。

这是供手动加载的学习版，不是 Chrome / Edge 商店安装包。加载后应保留原解压目录；移动或删除目录会影响扩展重新加载。只使用在线翻译不需要安装 Python、Node.js 或启动开发服务器。

本地 HTML 需在扩展详情中开启「允许访问文件网址」。浏览器设置页、扩展商店等受保护页面不能作为整页翻译验收页；PDF 和其他特殊内容也不属于本学习版的已验收范围。同一页面同时运行多个整页翻译扩展可能产生重复译文。

## 2. 先用 MyMemory 免 Key 试一句

MyMemory 是**有额度的在线机器翻译接口**，不需要账号、邮件或 API Key；它不是聊天 AI，也不是离线服务，不使用 AI 翻译 Skill。

1. 打开扩展设置，进入左侧 **AI 翻译向导**，找到顶部 **MyMemory · 免Key试用**。
2. 选择「英文 → 中文」或「中文 → 英文」，点击 **测试免费接口**。只会发送卡片中展示的合成句。
3. 若当前为「仅本机离线」，先点击 **切到屏蔽谷歌模式**。这一步允许非谷歌远程服务联网；仍需再次点击测试才发送示例句。
4. 测试成功后点击 **添加到翻译服务**。若提示已经在列表中，无需重复添加。
5. 刷新原阅读页面，打开工具栏翻译面板，选择 **MyMemory · 免 Key**，再点击网页悬浮按钮。测试与添加操作都不会自动更改全局默认服务。

首次安装的默认在线服务仍是上游 Microsoft，也可直接试用。选择哪个在线服务，待译文本就会发送到哪个服务。免费服务可能限流或变化，不能保证无限使用。MyMemory 额度耗尽或返回 429 时会停止本运行环境的待发队列；至少冷却 60 秒后可手动重试，每日额度用完可能需要等待更久。不会自动换平台。

8 家 AI API、自定义接口、费用说明及豆包/Kimi 实验性网页方式见 [AI 翻译服务说明](AI-SERVICES.md)。其中 8 家 API 尚未用用户真实 Key 调用，后台网页方式尚未完成真实登录会话验收。

## 3. 设置默认服务与双语显示

只调整当前页面：在网页翻译面板中选择已配置的服务或目标语言。已完成的旧译文会保留，等新译文成功后逐段擦除淡入；失败时保留旧文，并在段落上显示简短错误提示。系统开启「减少动态效果」时直接替换。

调整以后打开网页使用的默认值：

1. 扩展设置 → **网页翻译 → 全局规则**。
2. 点击规则底部的 **编辑**，选择服务、原文语言与目标语言。
3. 保持「仅显示译文」关闭，点击 **保存**。
4. 回到阅读页面并刷新。某个网站已有专用规则时，它可能覆盖全局规则，可在当前网页面板再次选择。

编辑同一服务的 Key、地址、模型或翻译偏好后，需要刷新原阅读页面才会载入新配置；这种配置编辑不属于动态服务切换。新安装默认使用简体中文界面、原文自动判断、目标简体中文，且不自动开启整页翻译。已有的按钮操作、界面和服务偏好会保留，升级不会强制覆盖。

## 4. 选择联网方式

在扩展设置 **概览 → 联网策略** 中选择：

| 方式       | 用途                                                                               |
| ---------- | ---------------------------------------------------------------------------------- |
| 正常联网   | 使用所选在线或本机服务                                                             |
| 屏蔽谷歌   | 拒绝谷歌请求，仍允许其他远程服务；可选择 Microsoft、MyMemory 等                    |
| 仅本机离线 | 插件内置 HTTP 请求层只允许明确的本机回环地址；使用已准备好的 Argos，远程服务会报错 |

“屏蔽谷歌”下 Microsoft 双向成功已有真实测试，但不代表中国所有运营商都可访问。“仅本机离线”约束插件请求层，不是整台电脑的防火墙，也不改变网页自身联网或撤回此前已发出的请求。切换策略后刷新阅读页；真正断网阅读还需提前打开或保存文章。

## 5. 真正离线：准备本机 Argos

**扩展安装包、源码包都不包含 Python 依赖与翻译模型。** 安装包根目录附带 `offline` 脚本；也可下载同一发布版本的 `kiss-translator-learning-source.zip`。在脚本所在项目先联网完成准备，再断网使用；只加载浏览器扩展不会执行这些准备步骤。初次准备、日常启动、停止、换机及 Windows 命令见 [离线使用说明](offline/README.md)。

准备并验证成功后，每次需要本机服务时，在源码项目根目录运行：

```sh
.offline/venv/bin/python offline/server.py
```

Windows PowerShell 使用：

```powershell
.\.offline\venv\Scripts\python.exe offline\server.py
```

保持终端运行，扩展中选择 **仅本机离线** 与 **本机离线 · Argos**。固定接口地址为 `http://127.0.0.1:8765/translate`，Key 留空。这是本机 API 地址，不是扩展安装网址。关闭终端或按 `Ctrl + C` 会停止服务；下次重新运行同一命令即可，不必重新下载模型。

只有中英模型，混合段落可手动选源语言。标签分段会影响上下文、语序和流畅度。Ollama 接口沿用上游，但本学习版未安装或实测，不能把云模型当作离线模型。

## 更新、备份与排错

升级前保留自己的设置备份。在同一已加载目录更新发布文件后，到扩展管理页点击重新加载，再刷新阅读页。只重新加载通常会保留设置；卸载、换浏览器用户配置文件或换电脑则不能依赖原本地存储。不要为了补一个缺失预置就清空全部设置。

按钮不出现、服务列表未更新、免费额度错误、本机连接失败等，见 [排错说明](docs/TROUBLESHOOTING.md)。已验证内容与限制见 [验证记录](VALIDATION.md)。

## 从源码学习和构建

以下仅面向开发者。以 Node.js 20、pnpm 11 和项目锁文件为当前构建参考；依赖安装需要联网。在源码根目录运行：

```sh
pnpm install --frozen-lockfile
pnpm build:chrome
```

然后按安装步骤加载生成的 `build/chrome`。本地 Web 开发演示须先运行下面的命令，才能访问所列地址；它们不是 GitHub 发布后的在线网站，也不是扩展使用所需服务：

```sh
HOST=127.0.0.1 PORT=4318 BROWSER=none pnpm start
```

开发页面：`http://127.0.0.1:4318/content.html`、`http://127.0.0.1:4318/free-api-demo.html`。Web 开发存储与已安装扩展的存储独立，后台消息、权限和跨域行为也不同。Windows PowerShell 可分别设置 `$env:HOST='127.0.0.1'`、`$env:PORT='4318'`、`$env:BROWSER='none'`，再运行 `pnpm start`。

阅读入口：`src/views/Action/ContentFab.js` → `src/libs/translatorManager.js` → `src/libs/translator.js` → `src/apis/trans.js`。固定模型指令见 [翻译 Skill](TRANSLATION-SKILL.md)。

基础为 KISS Translator 2.0.32，提交 `2656f564dde5b271a8d3fb31e951a8457e8df41c`。学习版继续采用 GPL-3.0，分发扩展时同时提供对应源码和 `LICENSE`；源码 ZIP 不含 Git 历史。沉浸式翻译仅用于交互参考，没有复制其当前产品实现。
