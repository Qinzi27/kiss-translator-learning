# 在 Chrome 中更新插件，或使用本地工具

本说明对应 **2.0.36-learning.7**。设置左侧新增 **插件更新**，可在 Chrome 内检查、下载、校验并写入新版。learning.6 附带的 Python 本地更新工具仍可使用。两种方式都只更新本学习版的扩展文件，无需登录 GitHub、API Token、Git、Node.js 或翻译模型。

| 方式            | 条件与入口                                                    | 备份位置                                      |
| --------------- | ------------------------------------------------------------- | --------------------------------------------- |
| Chrome 内更新   | 已加载的开发者模式扩展，浏览器支持文件夹授权；设置 → 插件更新 | 本扩展的 IndexedDB；单份旧文件内容最多 64 MiB |
| Python 本地工具 | Python 3.9+；双击 `更新插件.command` 或 `更新插件.bat`        | 安装目录旁的隐藏文件备份目录                  |

**不要同时运行这两种更新方式。** 它们的事务记录、备份和锁独立，不能互相协调；也不要在更新期间构建或手动改写同一安装目录。

## Chrome 内更新

这个入口用于 Chrome 的 **加载已解压的扩展程序** 安装。首次运行时会检查浏览器的 File System Access、IndexedDB 和更新锁支持；能力不满足或商店安装版会显示原因，不开始更新。它不新增扩展清单权限，也不需要本机 HTTP 服务或 Python，但所选目录的读写权仍必须由用户明确授权。

1. 保存未完成的阅读任务，在扩展设置左侧打开 **插件更新**。仅打开页面不会联网检查、请求目录授权或写入安装文件。
2. 点击 **选择并授权安装目录**，选择 Chrome 真正加载的 `chrome` 文件夹，里面应直接有 `manifest.json`；源码构建安装选择 `build/chrome`。不要选择外层目录、源码根目录或另一个同版本副本。浏览器再次询问时允许读写。
3. 插件向所选目录临时写入含随机 nonce 的纯文本文件，再从本扩展的 `chrome-extension://` 地址读取并比对，最后删除探测文件。匹配才会保存目录授权记录；同名同版本副本不能通过这项核对。探测不会执行文件或下载新版。
4. 点击 **检查更新**。找到更高且附件齐全的版本后，点击 **下载并更新**，保持当前设置页面打开。页面显示检查 / 下载、解压校验、建立备份和文件写入进度。
5. 更新完成后，明确点击 **重新加载插件**，再刷新正在阅读的网页和 PDF 标签。写入文件本身不会自动重新加载，运行中的版本和磁盘中的版本可能暂时不同。

安装目录保留在本扩展的 IndexedDB 中，浏览器可能要求再次确认文件权限。继续使用原目录和同一浏览器用户配置，通常可以保留原有设置；目录授权不表示允许访问其他个人文件夹。

### 写入顺序与恢复

浏览器会先校验下载并读取所有待改动文件，将旧文件内容和校验值可靠保存到 IndexedDB 后才开始写入。只处理新版发布包内的插件路径，以及本更新器上次管理、此次已移除的旧路径；不扫描或备份文章、私人 PDF、模型或浏览器用户配置。`options.js`、`options.html` 和 `manifest.json` 等入口文件最后写入。

**这是逐文件事务，不是整目录原子替换。** 单个文件使用浏览器的写入流提交，但多个文件之间可能短暂处于混合版本。正常写入失败或取消会尝试用已保存的备份恢复原文件；遇到外部程序修改过的文件，会停止恢复并保留备份，避免直接覆盖外部修改。

如果页面被关闭、浏览器退出、失电或恢复失败，下一次打开 **插件更新** 可能显示未完成事务。请先点击 **恢复未完成更新**，完成后再重新加载；有未完成记录时不允许继续升级或切换安装目录。不要清除扩展存储或卸载扩展，否则可能丢失 IndexedDB 备份。已有完整备份时，也可点击 **恢复上一版文件**；回退只恢复扩展文件，不回滚设置或译文缓存。

浏览器更新最多保留一份旧文件备份，文件内容合计 **64 MiB**；后续更新会替换上一份备份。Chrome ZIP 上限 32 MiB，解压内容上限 64 MiB，单文件上限 16 MiB，路径数量上限 2000。它不备份 `chrome` 外的 Python 工具，也不会升级这些工具自身。

### 网络与验证边界

检查和下载使用固定仓库 `Qinzi27/kiss-translator-learning` 的公开 GitHub 发布。附件下载允许 GitHub 跳转到受认可的公开附件 CDN，请求不携带登录凭据、Cookie 或翻译 Key；这个狭窄例外不改变翻译 API 的禁止重定向规则。更新不会读取登录 Cookie 或待译文档，也不使用或上传翻译 Key。

**仅本机离线** 会阻止浏览器更新页的联网检查与下载。目录核对使用本扩展地址，恢复 IndexedDB 中的备份无需访问 GitHub；切换联网策略本身不会自动检查版本。普通模式仍需网络能访问固定 GitHub API 和附件来源。

校验包括附件 SHA-256、长度、版本、对应源码记录及 ZIP 路径 / 类型 / 大小限制。它依赖 HTTPS 和同一发布者提供的校验记录，**不是独立签名**，不能防止发布者账号被接管后发布恶意版本。

**本版尚未完成真实 Chrome File System Access 的端到端验收。** 当前已用合成文件系统与 IndexedDB 事务夹具验证升级、备份提交顺序、回滚、取消、外部修改保护、同版本副本识别、探测清理及容量限制，并有界面测试与构建检查；这些不能代替真实授权、磁盘写入、失电恢复与重新加载的完整实测。详细证据以 [VALIDATION.md](VALIDATION.md) 为准。

## 保留的 Python 本地更新工具

从 **2.0.35-learning.6** 起，安装包附带此工具。它是独立的本地进程，双击后才联网，不会常驻或定时运行。需要 **Python 3.9 或更高版本**，且能访问 GitHub API 和附件来源；插件的「仅本机离线」策略不控制它。它不会读取文章、PDF、Cookie、浏览器配置或翻译 Key，也不会更改翻译服务。

### 日常使用

第一次安装或从旧版补入工具时，保留这样的目录结构：

```text
长期保留的安装目录/
  chrome/                 ← Chrome 已加载的扩展目录
    manifest.json
  updater/
    update.py
  更新插件.command         ← macOS 双击这个
  更新插件.bat             ← Windows 双击这个
  UPDATING.md
```

1. 保存未完成的阅读任务，然后双击对应系统的「更新插件」文件。终端会显示目标路径、已安装版本和检查 / 下载进度。
2. 工具找到更高版本后自动下载、校验、备份并替换。显示「已是最新版本」时无需操作；失败时根据提示处理，旧文件会保留或恢复。
3. 更新成功后，Chrome 打开 `chrome://extensions`（Edge 为 `edge://extensions`），在本学习版卡片点击 **重新加载**，再刷新阅读标签或重新打开 PDF。

**保留 Chrome 原来加载的目录路径，也保留原来的浏览器配置文件。** 不需要卸载或重新加载另一个路径。扩展本地设置通常会保留；重新加载会中止正在进行的翻译，也可能清除 PDF 会话译文缓存。关闭终端不会影响已完成的文件更新。

从本仓库源码运行启动器时，工具会检查源码标记并使用 `build/chrome`；它只更新构建目录，不做 `git pull`，不改源码、模型或启动器自身。以后重新从旧源码构建会覆盖下载的新版本，开发者应另行同步源码。

已经安装旧版但目录里没有工具：从新包把 `updater` 文件夹、两个启动器和本说明放到**原 `chrome` 文件夹旁边**，之后双击使用。不要把它们放进 `chrome` 内，也不要把启动器单独移动到桌面；可创建指向原启动器的快捷方式。

### Python 与运行方式

扩展本身和 Chrome 内更新不依赖 Python；此备用工具需要。没有 Python 时，启动器会报错并停止；可从 [Python 官方网站](https://www.python.org/downloads/) 安装。工具仅用 Python 标准库，不自动安装依赖，也不要求管理员权限。

macOS 在终端检查 `python3 --version`。如果解压工具丢失了启动器的可执行权限，可在安装目录终端运行 `bash 更新插件.command`；不需要修改系统安全策略。Linux 可直接运行下面的 Python 命令。Windows 启动器优先使用 `py -3`，否则使用 PATH 中的 `python`。

在安装目录终端中可执行：

```sh
# 只查询新版本，不替换文件（会联网）
python3 updater/update.py --check

# 开始更新
python3 updater/update.py

# 恢复上次更新前的文件（不联网）
python3 updater/update.py --rollback

# 扩展安装在其他位置时，明确指定含 manifest.json 的目录
python3 updater/update.py --target "/完整路径/chrome"
```

Windows 将 `python3` 换成 `py -3`。回退后同样需要在浏览器点「重新加载」，回退只恢复扩展文件，不回滚浏览器里的设置或缓存。不能依靠回退修复跨版本设置迁移，所以升级前仍可在插件设置中做配置备份；按安全设计，导出的设置不包含 API Key。

### Python 工具的下载验证和故障处理

工具只接收 `Qinzi27/kiss-translator-learning` 发布的 `v数字-learning.数字` 版本，包含本项目的预发布。只有同时提供 Chrome ZIP、对应源码 ZIP、`release-manifest.json` 和 `SHA256SUMS.txt` 的发布才参与选择；草稿不参与。按版本号选取更高版本，不会把已经安装的开发新版降级。发布尚未传齐附件时，继续使用现有版本，稍后再运行即可。

下载过程中核对清单的 SHA-256、包长度、版本、干净源码提交记录与 ZIP 内清单。源码包必须与发布记录一致，但更新时只下载 Chrome ZIP 和两份小型校验文件，不下载源码包。提取前检查路径、符号链接、文件名冲突和解压限额，完成验证后才替换目标目录。只保留最近一次扩展文件备份，并防止同时运行两个 Python 更新任务；它的锁不能阻止 Chrome 内更新，因此必须由用户避免两种方式同时运行。正常异常或中断会尽力恢复旧文件，遇到上次未完成的 Python 事务会先恢复再继续。

校验依赖 HTTPS 和同一 GitHub 仓库的发布记录，**不是独立签名，也不能防止发布者账号被接管后发布恶意版本**。不会执行安装包中的脚本来完成更新，也不会自动升级启动器或 Python 更新器自身。安装包可能附有新版文档 / 更新器，当前工具只替换 `chrome` 内容；需要升级工具本身时，应另外取用经过审阅的新工具。

限制为最多读取最近 300 条发布记录，网络与解压共用 180 秒截止时间；安装包最多 96 MiB，解压内容最多 256 MiB / 10000 项，单文件最多 64 MiB。只支持普通单卷 ZIP，不接收 ZIP64。目标旁的隐藏 `.chrome.learning-backup` 为单份备份，`.chrome.learning-update.json` 为事务记录，锁文件会保留但操作系统会在退出后释放锁。安装目录名不是 `chrome` 时前缀随目录名变化。强制中断留下的 `.chrome.learning-stage-*` 暂存目录只提示，不会按名称擅自删除；确认没有其他更新器运行后可检查并手动清理。

网络超时、GitHub API 限流、校验不符或磁盘空间不足时，工具会报错停止。GitHub API 对匿名访问有限额；稍后重试即可，不要向更新器填写账号密码或 Token。若浏览器或安全软件锁住安装文件，可先关闭 Chrome / Edge 后重试。回退不能代替独立的数据备份；强制关机或磁盘故障后以工具恢复提示为准。

两种方案都需要在文件更新完成后明确重新加载插件。普通已解压扩展不能靠 `update_url` 获得 Chrome 托管的后台更新；商店发布与受管理环境的分发方式见 [Chrome 官方说明](https://developer.chrome.com/docs/extensions/how-to/distribute)。发布信息接口见 [GitHub Releases 官方文档](https://docs.github.com/en/rest/releases/releases#list-releases)。
