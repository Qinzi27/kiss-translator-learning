# 安全与升级说明

适用版本：**2.0.35-learning.6（预发布）**，检查日期：2026-09-15。安装目标是 Chrome / Edge 开发者模式；Chrome 151+ 的官方 PDF MIME 接管是新增路径。learning.4 的安全修复继续保留，建议更早学习版用户更新文件、重新加载扩展并刷新阅读页。

## 延续的安全修复

- 将网页可派发的消息与扩展内部操作分开；普通网页不能通过公开消息修改服务、规则或执行脚本。注入界面的操作、快捷键和划词入口拒绝合成输入事件，内部划词消息改用模块内通信。
- 停用规则的 `injectJs`、`transStartHook`、`transEndHook` 和 API 的 `reqHook`、`resHook`，移除 Sval 执行依赖。旧脚本文本仍保留，但不会运行。普通翻译、AI 提示词/术语偏好、Custom 默认 JSON 协议与 Argos 不依赖这些脚本。
- 加密同步拒绝明文降级、损坏内容和错误口令，不覆盖本机设置。发送设置前进行白名单脱敏；远端修改地址或删除服务不能把本机凭据绑定到新地址或清空含私密配置的本机服务。
- 后台网页 AI 发送前核对输入框仍连接、完整提示未改变、编辑区域唯一，发送按钮必须属于同一明确区域；页面重绘或草稿/按钮歧义时停止发送。
- Argos 翻译接口默认要求本机配对令牌，校验回环 Host 和 Origin，限制正文大小、读取时间、连接数和推理并发。
- 所有内置 HTTP 请求的远程地址必须为 HTTPS，禁止重定向；HTTP 仅允许明确的 `localhost`、`127.0.0.1`、`[::1]`。开发预览绑定回环地址，移除通配 CORS 并升级开发服务器。
- 升级 HTML 清理、XML/选择器解析、路由和构建依赖。锁文件对应依赖的 npm 审计结果见 [验证记录](VALIDATION.md)。

## 更新后需要做什么

**Argos：** 先停止旧服务，使用本版安装包或源码包更新 `offline` 脚本，保留原 `.offline` 模型/虚拟环境目录，再启动新服务。把终端显示的配对令牌填写到 Argos 服务的 **Key** 字段并保存。旧版留空的 Key 会收到 401。令牌生成在 `.offline/pairing-token`，不要上传或分享；POSIX 上文件权限为 `0600`。非交互启动、换机和令牌轮换见 [离线说明](offline/README.md)。

**设置导出和同步：** 共享的是常用偏好与翻译服务骨架；不包含 Key、任意 headers/body、JavaScript Hook、提示词、未知字段和订阅 URL。含用户信息、查询参数、片段或特殊路径的端点也不会共享。联网策略只在本机保存，新设备需要自行补充端点、提示词和凭据，并核对模型。带 `safe-v1` 标记的设置备份导回本机时会合并保留已有凭据。用户显式导入旧的本地备份仍兼容，旧文件可能含明文密钥，应自行妥善保管。

旧明文云备份不会自动迁移：先自行下载核实内容；云文件通常包在 `{key,value,updateAt}` 中，设置导入需要取出 `value` 并按需要解析为设置对象，不能直接导入整个云文件。保留原备份，显式导入本机后改用新的空同步位置，建立新的加密同步；继续使用原明文文件仍会被拒绝。远端未匹配或改址时，本机含私密配置的服务会保留；要删除该项，请在本机设置中明确删除。个人规则、生词等仍使用认证加密同步，不能把“设置脱敏”理解成对任意规则文本和生词内容的脱敏。

**自定义脚本：** 本版仅保留脚本文本供学习和未来迁移，任何配置都不能重新开启执行。复杂第三方协议需要在自己控制的服务端实现适配；不要依赖历史 Hook 示例。油猴外置设置页的 GM 存储/请求桥已停用，不能继续用该页面管理油猴配置。油猴跨域请求改为原生 fetch，会受到服务端 CORS 限制。

**HTTP 服务：** 修改为最终 HTTPS 端点；短链接、重定向端点和远程明文 HTTP 将被拒绝。本机服务也不允许重定向。

## 数据与权限边界

API Key 仍保存在浏览器扩展本机存储中，不是操作系统加密保险库；本机账号、浏览器配置文件和扩展环境需要可信。在线翻译会向所选服务发送待译文本。网页 AI 使用正常浏览器登录会话，不导出 Cookie；网站仍可能保存会话与提示。模型准备完成后，Argos 推理可留在本机。

联网策略只约束插件内置请求层，不是整台设备或网页的防火墙。网页界面与文章处在同一个阅读页面，不能把界面的显示当作独立于页面的可信认证提示。后台网页登录兼容性、8 家云 API 的真实账号权限及最终安装环境均需要单独验收。

审计和回归测试说明了已检查的范围，不能证明未来或所有环境中不存在漏洞。当前没有附带共享云端 Key、模型或用户账户数据。

## PDF 阅读器与会话数据

本版使用固定版本 Mozilla PDF.js **6.3.289** 的官方兼容构建。解析主库、Worker、字体和字符映射随扩展打包，不依赖 CDN。阅读器仅调用文字提取和画布绘制，不实例化 PDF 脚本管理器、交互表单或批注执行层；关闭 XFA 与 Wasm，不打包 QuickJS 和 sandbox 模块，不放宽默认 CSP。兼容库中的固定环境探测代码不等于执行 PDF 内嵌 JavaScript。

Chrome 151+ 通过新增的 `mime_types_handler` 声明接管顶层 PDF，不启用内嵌文档接管。地址栏保留原 URL，阅读器读取 Chrome 已接收的响应流，默认只显示原文，不自动外送。只有浏览器创建的处理框架中成功调用 `getStreamInfo()` 才能提供该流；核对流 URL 属于本扩展后单次读取，不接受网页传入的流 URL，不重新请求原地址。此路径的本地 PDF 无需文件网址权限。用户可关闭接管或通过官方接口返回原生阅读器；完整接收前返回原生时，Chrome 可能重新加载原文档。[官方 API 边界](https://developer.chrome.com/docs/extensions/reference/api/mimeHandler)

旧浏览器的手动入口直接读取 `file:///…pdf` 时，仍要求扩展已获文件网址访问权限且本地路径校验通过，拒绝 UNC / 远程主机和非 PDF 路径；用户通过文件选择器选择文件则无需该权限。手动 HTTP(S) URL 下载不发送 Cookie、Key 或文档 Referrer，并遵守现有 HTTPS、禁止重定向及联网策略。读取限时 45 秒，解析限时 90 秒，并限制文件体积、页数、文字及文本项数量。不能直接下载的登录或跳转链接可由用户正常下载后选择文件。

只有主动开启翻译才将抽取文字逐段发给所选服务，不发送文件名或来源 URL，不使用跨文档聊天上下文。「翻译本页」同时开启翻页自动翻译并授权预翻译后两页，最多并发 2 个请求。翻页只重排待处理任务，不取消已在途请求；停止、服务错误或新文档会终止该轮处理并忽略迟到结果，不会自动更换服务。全文任务优先当前页，再处理其余页，命中相同配置缓存的段落不重复发送。API 提示词与所选服务自身的数据留存规则仍适用。

阅读器不使用通用持久翻译缓存，新增专用会话缓存：扩展使用 `storage.session`，LRU 合计限制为 80 页、8 MiB，超限淘汰旧项。缓存仍含文章译文，不是加密保险库。按文件和服务配置区分结果；刷新恢复已完成内容不会自动续发翻译，手选文件需重选同一文件才能命中。浏览器会话结束清除扩展会话数据；网页预览使用 `sessionStorage`，浏览器恢复标签时可能保留，不能提供相同的会话清除保证。私人 PDF、用户译文及会话缓存不作为公开发布附件。

会话恢复提示还包含文件摘要、已授权的来源 URL 与页码；来源可能包含本地路径或链接查询参数。原 PDF 字节、原段落和服务凭据不写入这份会话缓存，配置只保存摘要。单个阅读器的显示结果另限 80 页、8 MiB 内容，单段译文超过 16,000 字符时不显示、不写缓存并停止该轮任务。这些是内容预算，JavaScript 对象、画布及浏览器自身还有额外内存开销。

旧版手动同标签入口仍由扩展按钮或原生快捷键 / 菜单授权：随机启动标记绑定标签 ID，临时保存在 `storage.session`，两分钟内有效且使用前删除。普通外部链接或 `#url` 参数不能授权自动下载、翻译；不支持会话存储时降级为手动预填。普通打开另一文件不会继承自动翻译授权，明确点击「选择 PDF 并翻译」则授权对新选文件开始翻译。此入口没有新增可由网页派发的后台消息。

联网策略约束插件主动发出的下载和翻译请求，不控制浏览器原本打开 PDF 的网络请求；读取已由浏览器提供的 MIME 流也不会撤回此前的导航。扫描 OCR、译文 PDF 导出尚未接入。完整边界和第三方许可证见 [PDF 阅读说明](docs/PDF-READER.md)。本轮新增路径使用合成测试与构建检查，Chrome 153 实际安装后的 MIME 完整流程尚未实测；早期 MyMemory / Argos 和 Web 预览记录不能代替此验收，详见 [验证记录](VALIDATION.md)。

## 本地更新工具

learning.6 新增的 Python 更新器与插件进程独立，只在用户运行启动器或命令时联网。它不读取浏览器配置、Cookie、API Key、原文或 PDF，不启动本地 HTTP 服务，也不接受网页控制。插件的离线联网策略不约束这个独立程序；真正断网时无法下载更新，但不影响已有扩展文件。

来源固定为本学习版的公开 GitHub Releases，包括完整的预发布；仅允许所需 HTTPS 发布地址和 GitHub 附件重定向。校验发布清单、SHA-256、大小、版本和 ZIP 内的学习版身份；提取前拒绝路径穿越、符号链接 / 特殊文件、名称冲突和超限压缩包，只替换明确的扩展安装目录。使用同级暂存、互斥与事务恢复，保留一份旧文件供回退，不执行下载包附带的更新脚本。工具本身不会自动升级。

这些校验依赖同一仓库的 HTTPS 发布信息，**不等于独立代码签名或对发布者账号失陷的防护**。回退恢复的是扩展文件，不能恢复此前版本在浏览器里更改的设置。系统强制断电、磁盘故障或恶意本地用户不在普通异常回退的保证范围内。操作和验证边界见 [更新工具说明](UPDATING.md) 与 [验证记录](VALIDATION.md)。

## 报告问题

请通过本仓库 [GitHub Security](https://github.com/Qinzi27/kiss-translator-learning/security) 查看可用的私密报告方式。公开 Issue 中只提供版本、浏览器和不含敏感数据的复现步骤；不要粘贴 API Key、Cookie、完整设置备份或私密文章。无法私密提交时，先询问维护者联系渠道。
