# 2.0.37-learning.8 · 固定按钮位置与长按持续翻译

基于 fishjar/kiss-translator 2.0.32 的非官方 GPL-3.0 学习版。本次预发布修正网页悬浮按钮首次出现的位置，并新增长按切换持续翻译锁定。Chrome 内更新、三种状态颜色与 PDF 阅读器继续保留。

## 本次更新

- **首次绘制就恢复位置。** 网页悬浮按钮直接使用保存的位置，避免先出现在角落再移动。换网页仍会在新文档中重新注入按钮，原位置、三种颜色与点击偏好保留；切换目标语言仍是当前页翻译操作。
- **长按 650 毫秒切换持续翻译锁定。** 小锁徽标、标题和状态播报显示保存结果；失败时提示重试。移动、拖拽、提前松手、取消或失焦会取消未完成的长按，成功长按后的松手不会额外触发单击。按钮获焦后可按向下箭头或 `Shift + F10` 打开菜单，用键盘切换锁定。
- **单击管当前页，锁定管后续网页。** 启用锁定也会启用当前页翻译；普通单击只开关当前页，不改变锁定，旧菜单点击偏好仍保留。解锁保留当前页译文。后续普通顶层网页、刷新与单页应用新路径按锁定决定是否自动开启；未锁定时，旧规则中保存的自动开启状态也不会开启新网页。
- **锁定单独保存在本机。** 当前浏览器配置中的持久锁使用独立存储项，拖动位置或调整颜色不会覆盖它。其他已打开标签只同步锁标记，不突然发送文本；网站黑名单和联网策略继续生效。iframe 和 PDF 不会因网页锁定而自动翻译。使用在线服务时，锁定后的新网页会发送待译文本并使用相应额度。

## 下载与使用

- `kiss-translator-learning-chrome.zip`：扩展、许可证、文档及保留的 Python 更新工具。首次安装仍加载其中的 `chrome` 文件夹。
- `kiss-translator-learning-source.zip`：对应 GPL 源码与测试，不含依赖、模型、个人文件或账号配置。
- `SHA256SUMS.txt` 和 `release-manifest.json`：附件校验值、版本及对应提交。

已有 learning.7 的用户可打开插件设置 → **插件更新**。首次选择并授权 Chrome 当前加载的 `chrome` 文件夹，点击 **检查更新 → 下载并更新 → 重新加载插件**，再刷新阅读标签。更早版本可使用原 Python 工具或手动更新。首次升级到 learning.8 同样需要重新加载并刷新，默认不启用持续翻译锁，保留已有位置与配色。

具体步骤见 [更新说明](https://github.com/Qinzi27/kiss-translator-learning/blob/v2.0.37-learning.8/UPDATING.md) 和 [安装说明](https://github.com/Qinzi27/kiss-translator-learning/blob/v2.0.37-learning.8/START-HERE.md)。

## 验证与边界

本轮自动回归涵盖首次渲染位置、真实按钮与拖拽组件组合中的长按、取消、键盘操作和合成事件拒绝。自动测试不能替代真实 Chrome 安装后的跨页持久锁定验收；本轮没有新增用户 Key 或云模型 API 的实调。完整结果见 [VALIDATION](https://github.com/Qinzi27/kiss-translator-learning/blob/v2.0.37-learning.8/VALIDATION.md)。

更新面板只用于开发者模式安装；文件选择授权由 Chrome 决定。浏览器内更新采用逐文件提交与最多一份 64 MiB 的本机 IndexedDB 备份，不是整个目录原子替换；中断时尝试恢复，强制关闭或失电可能需要下次打开面板手动恢复。更新时保持页面打开，不要同时运行 Python 更新工具。重新加载后刷新阅读标签，PDF 会话缓存可能被清除。

仅本机离线模式不会联网更新；GitHub 不可达时保留当前文件。校验信息来自同一仓库，不是独立签名。本轮没有新增权限、依赖或共享 Key。既有真实公开下载与解包记录仍可查阅；Chrome 原生文件授权 / 写入 / 重新加载的完整流程，以及 PDF MIME 实机接管、Windows 启动器仍保留实机验收边界。
