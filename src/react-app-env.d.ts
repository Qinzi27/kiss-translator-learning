// The first migration covers browser-only, framework-independent modules.
// Ambient Node/React types are intentionally not loaded into this boundary.
// Add explicit, compatible type dependencies when migrating their consumers.
export {};
